<% provide(:title, "Edit App") %>
<h1>Update Application</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for( [@user, @user.volunteer_app] ) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Save Changes", class: "btn btn-large btn-primary" %>  
    <% end %>
  </div>
</div>
