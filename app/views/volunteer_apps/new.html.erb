<% provide(:title, 'Apply') %>
<h1>My Application</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for( [@user, @user.build_volunteer_app] ) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Submit", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
